spring.application.name=marketplace
# --- Datasource ---
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=marketplace;encrypt=true;trustServerCertificate=true
spring.datasource.username=market_user
spring.datasource.password=MarketPass1234
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- File Upload Configuration ---
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=25MB
file.upload-dir=uploads/products
file.upload-dir-absolute=${user.dir}/uploads/products

# --- JWT Configuration ---
jwt.secret=UON-Marketplace-Super-Secret-Key-For-JWT-2024-Change-In-Production-Min-256-Bits-Required-For-HMAC-SHA256
jwt.expiration=86400000

# --- Two-Factor Authentication ---
# Rate limiting for 2FA attempts (future enhancement)
# 2fa.max-attempts=5
# 2fa.lockout-duration=300000

# --- Mail Configuration (Gmail SMTP) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=uonmarketplace@gmail.com
# Note: App Password pasted without spaces
spring.mail.password=vlopobuljoxrjzei
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
